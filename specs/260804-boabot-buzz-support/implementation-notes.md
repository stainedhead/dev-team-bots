# Implementation Notes: BaoBot Buzz Support

**Feature:** boabot-buzz-support
**Created:** 2026-08-04

**Purpose:** Running log of technical decisions, edge cases, deviations from plan, and lessons learned during implementation. Update this file as work proceeds — do not wait until the end.

---

## Technical Decisions

Decisions made during PRD/spec pre-flight, recorded here so implementation doesn't re-litigate them:

- **Scope:** full PRD, both workstreams (Buzz + Secret Storage), implemented in one dev-flow run — user's explicit choice over "Buzz P0 only."
- **OQ-1 (multi-instance singleton):** resolved as a process-level lock on the nsec (PRD option (a)), not a startup presence probe (b) or reply dedup (c). FR-031 is implemented and tested, not deferred.
- **Untestable acceptance criteria:** implemented as `//go:build integration` tests, not run automatically in this job. Flagged in this file (see below) for manual verification against real infrastructure.
- **OQ-9 (secret namespace key):** bot **name**, not bot type — consistent with `BotName` usage elsewhere in the codebase.
- **Other non-blocking open questions (OQ-2, OQ-3, OQ-4, OQ-5, OQ-6, OQ-8, OQ-10, OQ-11):** not resolved with a hard decision — implementation proceeds per the PRD's own stated lean where a concrete choice is unavoidable (e.g., OQ-2 doesn't block implementing FR-005–007 since attestation *validation* doesn't require deciding the *issuance* workflow); genuinely deferred items (OQ-5 `boabotctl` attestation tooling, OQ-6 NIP-AE engrams) remain out of this implementation's scope per the PRD's own P1/P2 phasing.
- **OQ-11 resolved for `boabotctl secret` (FR-049):** local machine only, no remote-bot writes. Simpler and matches the PRD's stated lean.

## Decisions made during spec review (Step 2)

Recorded here so Step 3 implementation doesn't have to re-derive them from the review conversation:

- **Scope resolution (spec.md/plan.md/tasks.md were inconsistent before this review):** the full in-scope FR range for this run is FR-001–FR-054, including FR-025 (typing), FR-026 (gated `!shutdown`), and FR-027 (reactions) pulled forward from PRD P1 — `plan.md`'s Phase F already implemented them, and leaving them out while implementing everything else in Phase F would have left in-progress code contradicting the phasing doc. FR-048 is in scope for its warn-only clause only; the post-deprecation hard-rejection trigger is deferred one release, per the PRD's own P2 phasing. See `spec.md` §Timeline and Milestones and `tasks.md` §Deferred Items for the authoritative list.
- **`domain.Event`/`domain.Filter` are domain-owned types, not `fiatjaf.com/nostr`'s library types.** `data-dictionary.md` previously left this "TBD, likely reused directly" — that would fail the PRD's own `grep -r "fiatjaf.com/nostr" internal/domain` acceptance criterion. `internal/infrastructure/buzz/relay_client.go` now owns a translation layer between the two, itself a tested component (`tasks.md` D3).
- **`boabotctl`'s secret subcommands (FR-049) cannot share code with `boabot`'s `internal/infrastructure/secret/keystore`** — different Go module, and `internal/` visibility is per-module-root. `boabotctl` takes its own `zalando/go-keyring` dependency and only shares the FR-045 key-naming *convention* with `boabot`, not code. See `architecture.md` §Cross-Module Constraint.
- **Edge cases with no prior design note, now resolved** (see `architecture.md` §Edge Cases and Failure Paths for full detail): FR-022 reply-publish failure (log, don't re-enqueue — the worker already ran); per-secret-provider timeout (2s per provider via `context.WithTimeout`, a timeout is a miss not an error); the pending task-ID map is untouched across reconnect (only connection/auth/subscriptions are re-established); the presence ticker suspends while disconnected and resumes on reconnect; `respond_to_allowlist` nil (no gate) vs. explicit empty list (allow-none) are distinct configurations.

## Phase B — Secret storage domain + providers (complete)

**B1 spike findings** (resolves `research.md`'s two `[needs implementation research]` items scoped to this phase):

- **`zalando/go-keyring` (confirmed at `v0.2.8`, the actual latest via `go get github.com/zalando/go-keyring@latest` — matches the PRD's own research):**
  - `keyring.Get(service, user string) (string, error)`, `keyring.Set(service, user, password string) error`, `keyring.Delete(service, user string) error`, `keyring.DeleteAll(service string) error`; `keyring.ErrNotFound` and `keyring.ErrSetDataTooBig` sentinel errors. Confirmed by reading `keyring.go` in the module cache.
  - **Darwin write path verified stdin-only, never argv:** `keyring_darwin.go`'s `Set` runs `exec.Command("/usr/bin/security", "-i")`, opens a `StdinPipe`, and writes the full `add-generic-password ...` command (with the password base64-encoded and shell-escaped) to that pipe — the password is never a `Cmd.Args` element. `Get`/`Delete` use `-s`/`-a` flags with the service/user only (never the secret). This directly satisfies FR-052 for the one backend that shells out at all; Linux (D-Bus via `godbus/dbus`) and Windows (`danieljoos/wincred` syscalls) never invoke a subprocess.
  - The library ships its own test seam, `keyring.MockInit()` / `keyring.MockInitWithError(err)`, which swaps the package-level backend for an in-memory map — used throughout `internal/infrastructure/secret/keystore`'s tests so no test ever touches a real OS keystore or spawns `/usr/bin/security`.
  - Since our own `keystore.Provider` never constructs an `exec.Command` itself (it only calls the library's package-level functions), FR-052's "no subprocess argument" AC is satisfied by (a) the library-source verification above and (b) a mechanical regression test, `TestProvider_SourceNeverConstructsASubprocessDirectly`, that scans the package's own non-test `.go` source for `os/exec`/`exec.Command` and fails if either appears — guarding against a future edit reintroducing a direct subprocess call here.
- **systemd `$CREDENTIALS_DIRECTORY` semantics (confirmed via `https://systemd.io/CREDENTIALS/`):** one regular file per credential, named after the credential ID, under the directory named by `$CREDENTIALS_DIRECTORY`; access restricted to the service's user, enforced by the kernel. The documentation does **not** specify a trailing-newline convention for file content. **Decision:** the systemd provider defensively strips a single trailing `\n` (credentials are often authored by hand or via `echo`/`printf` into `SetCredentialEncrypted=` inputs) and treats an unset `$CREDENTIALS_DIRECTORY` and a missing/empty-after-trim file as ordinary misses (not errors) — a genuine read error (e.g. the credential path unexpectedly being a directory) is still surfaced as an error, not swallowed. Live verification of exact byte-for-byte behavior against a real systemd unit remains on the manual-verification checklist below (`tasks.md` I2).

**Design decisions (B2–B10), recorded so later phases don't re-derive them:**

- `internal/domain/secret.go` contains exactly `SecretRef`, `SecretProvider`, `SecretStore` (per the task's explicit enumeration) — zero infrastructure imports, only `context`. The concrete ordered-chain implementation (`Store`, implementing `domain.SecretStore`) and its `NotFoundError` type live in `internal/infrastructure/secret/store.go` instead, since FR-038 only requires the *interfaces* to live in domain and the chain-composition logic, while dependency-free itself, is orchestration that belongs with the providers it composes.
- **Per-provider timeout is enforced at the `Store` call boundary via a goroutine + `select`, not by relying on providers to observe `ctx`.** All four real providers are synchronous local calls (env var read, file read, `os.ReadFile`, or a `zalando/go-keyring` call) that do not thread `ctx` into anything cancellable. Wrapping each `Lookup` call in its own goroutine with a `context.WithTimeout` select means a genuinely hung provider (e.g. an unresponsive D-Bus round trip) cannot block the chain past the timeout, at the cost of a leaked goroutine in that specific failure case — an accepted, documented tradeoff (`store.go`'s `callProvider` doc comment).
- **Key/namespacing convention (FR-045, threaded through B4–B7 per B8):** bot **name** (not type, per OQ-9). `env`: ignores `Bot` entirely (FR-044 — env vars are process-global; the var name is `strings.ToUpper(ref.Name)`). `file`/`systemd`/`keystore` are all **strict-match, no fallback**: when `Bot` is set, only the bot-scoped key (`"<bot>_<name>"` for file/systemd; service `"boabot"` + account `"<bot>/<name>"` for keystore) is consulted; when `Bot` is empty, only the bare `"<name>"` key is consulted. **Deliberately no fallback from a bot-scoped lookup to the global key** — an earlier draft had the file provider fall back to the global key when the bot-scoped one was absent, which would let a per-bot secret (e.g. a Buzz nsec) silently resolve to a differently-scoped global entry, meaning two bots could end up unknowingly sharing one private key. Caught during self-review (advisor pass) before commit; fixed to match systemd/keystore's already-strict behavior, with a regression test (`TestProvider_Lookup_BotSet_DoesNotFallBackToGlobal`) added. These conventions are documented in each package's doc comment and are the stable on-disk/on-keychain contract per FR-045.
- **FR-052 verification is two-layered, not source-grep alone.** An earlier draft's only FR-052 test scanned the keystore package's own source for `exec.Command`, which proves this package never shells out but says nothing about the argv zalando/go-keyring itself constructs — a future library upgrade that regressed darwin's `Set` to pass the password via `-w <password>` on argv would have left that test green. Fixed by adding `TestProvider_Set_ConstructsCallWithSecretOnlyInPasswordSlot`, which injects a recording fake `backend` and asserts the secret only ever lands in the library's dedicated `password` parameter of `Set(service, user, password)`, never spliced into `service` or `user` (the two arguments that *do* appear as `-s`/`-a` argv flags on darwin). Combined with the B1 spike's direct reading of `keyring_darwin.go` (documented above), this covers both the boundary we control (our own call construction) and the boundary we don't (the library's own transport, which is out of scope to re-verify per-run but is now unit-tested with a stronger regression signal at our seam). The source-grep test (`TestProvider_SourceNeverConstructsASubprocessDirectly`) is kept as a cheap complement.
- **`keyring.MockInit()`/`MockInitWithError()` are process-global state** (they set a package-level variable inside `zalando/go-keyring`). Every test that calls `MockInitWithError` does so via a small `mockInitWithError(t, err)` helper (in both `keystore/provider_test.go` and `no_value_logging_test.go`) that registers `t.Cleanup(keyring.MockInit)`, so a failing backend never leaks into whatever test runs next in the same binary — removing an ordering dependency between tests in these files.
- **FR-043's "world-readable file remains fatal" is satisfied at the provider level, not specially by `Store`:** `file.Provider.Lookup` returns `credentials.Load`'s error unchanged (verified by test to preserve the exact message and remediation hint) rather than downgrading it to a miss. `Store.Get`'s generic FR-039 behavior (any provider error is non-halting, chain continues) still applies uniformly — no special-casing for the file provider inside `Store`. The full system-level guarantee that a world-readable file refuses process startup is delivered by Phase C's `main.go` wiring (C1, out of scope for Phase B), which can recover the original message via `errors.As` against `Store`'s `NotFoundError.Unwrap() []error`.
- **`NotFoundError`** (FR-053) names the reference (`Name`, `Bot`) and enumerates every provider consulted in order; it also implements `Unwrap() []error` so callers can `errors.Is`/`errors.As` into the specific underlying provider errors it collected, without those errors' text needing to appear verbatim in `NotFoundError.Error()`'s own message (though it does include them for operator-facing diagnostics too).
- **FR-051 (provider half):** none of the four providers call any logger themselves; only `Store` logs (`provider=<name> timeout` / `provider=<name> error`), and only ever with the provider name and reference name, never a value. Verified by `internal/infrastructure/secret/no_value_logging_test.go`, a cross-provider test driving all four real providers' error paths (plus a hit path) through a captured `slog` buffer with a sentinel secret value, asserting the sentinel never appears in log output or in any returned error string — complementing each provider package's own narrower version of the same assertion.
- **Coverage:** 100% of statements on `internal/domain` (the new `secret.go` — trivial, interfaces only) and on all of `internal/infrastructure/secret/{,env,file,keystore,systemd}`.

## Phase C — Secret storage callers (complete)

**Design decisions (C1–C5), recorded so later phases don't re-derive them:**

- **C1 (`cmd/boabot/main.go`):** the two former `applyCredential` calls are replaced by `resolveEnvCredentials`, which calls a new `applyCredentialFromStore(ctx, store, secretName, envKey)` helper against a `secret.Store` built by `buildSecretProviders()` (env → systemd → keystore → file, matching FR-040's default order). Precedence ("an explicit env var always wins") is **not** re-implemented in `main.go` — it falls out of `Store`'s own provider ordering, since `env` is always tried first and returns on a hit. `applyCredentialFromStore` is therefore a thin `Get`-then-`Setenv` wrapper; when the store misses, `envKey` is left exactly as it was (matching pre-migration behaviour byte-for-byte). The pre-existing "world-readable `~/.boabot/credentials` is fatal at startup" guard is preserved as a literal, unconditional `credentials.Load` call inside `buildSecretProviders()` — run *before* the file provider is ever added to the chain and independent of which individual secrets are actually needed — because delegating that guard to lazy chain resolution would only trigger it when every earlier provider (env, systemd, keystore) had already missed, silently skipping the check whenever `ANTHROPIC_API_KEY`/`BOABOT_BACKUP_TOKEN` were already supplied via env var. If `credentials.DefaultPath()` itself fails (home dir undeterminable), the file provider is omitted from the chain entirely rather than passed an unusable path — matching the old code's behaviour of skipping the whole credentials-file block in that case.
- **Accepted behaviour delta (not a regression, but worth flagging):** for a credentials-file-only deployment, secret resolution now consults `systemd` and `keystore` before `file` on every startup — a new (inert, but non-zero-cost) `$CREDENTIALS_DIRECTORY` env read and an OS keystore round trip (a `/usr/bin/security` subprocess on macOS) that never happened pre-migration, plus up to 2s×2 of stall if either is unresponsive. The chain order is not negotiable (FR-040), and the "byte-identical **config**" AC (FR-046) still holds — this is a startup-cost/robustness note, not a functional regression. Cross-referenced with the existing macOS-LaunchDaemon keystore-reachability item on the manual-verification checklist below, since that's the scenario most likely to surface a slow/unresponsive keystore call at boot.
- **C2 (`SlackConfig.ResolveSecrets`, in `internal/infrastructure/config/config.go`):** a new method on `*SlackConfig` takes `(ctx, domain.SecretStore, *slog.Logger)`. When `BotToken`/`AppToken` are already set inline, they win outright and a deprecation warning is logged naming the alternative (a `~/.boabot/credentials` entry or an OS keystore secret) — this is also C3's FR-048 warn-only clause. When an inline field is empty, it resolves via `store.Get(ctx, domain.SecretRef{Name: "slack_bot_token"|"slack_app_token", Bot: s.BotName})`; a store miss just leaves the field empty (not an error), matching today's "monitor only activates when all three of BotToken/AppToken/BotName are present" gate in `main.go`. **Guard added after self-review:** `ResolveSecrets` skips the store entirely when `BotName == ""`, since Slack can never activate without one anyway (`main.go`'s activation check requires all three) — without the guard, every non-Slack bot's startup would pay for two unnecessary keystore/systemd round trips for secrets that could never be used.
- **C3 (FR-048 warn-only clause):** no new production code — `SlackConfig.ResolveSecrets` (C2) is config loading's only caller of the inline-Slack-token path, and its deprecation-warning branch *is* the warn-only clause in full. Verified by a dedicated test (`TestSlackConfig_ResolveSecrets_InlineFieldsWin`, `internal/infrastructure/config/slack_secrets_test.go`) asserting the warning text names the alternative. The post-deprecation hard-rejection trigger remains out of scope this run (`tasks.md` §Deferred Items).
- **C4 (diagnostic surface — `cmd/boabot/diag.go`):** implemented as a `--diag-secrets` flag on the existing `boabot` binary rather than a new subcommand or separate tool, since `main.go` already parses flags with the stdlib `flag` package (no `cobra` dependency exists in this module) and the diagnostic needs the same config file and provider wiring as a normal run. `main()` loads `config.Config` once and branches on the flag before doing anything else. `diagnoseSecrets` deliberately does **not** call `provider.Lookup` directly — for each `(ref, provider)` pair it builds a throwaway single-provider `secret.Store` (`secret.New([]domain.SecretProvider{p}, secret.WithLogger(logger))`) and calls `Get`, so the diagnostic inherits `Store`'s per-provider timeout and non-halting-on-error semantics exactly, without any change to the already-merged Phase B `store.go` (out of this phase's scope). Output is one line per configured secret naming only the ref and the resolving provider (or, on a miss, every provider consulted) — the resolved value is discarded immediately inside `diagnoseSecrets` and never flows to `printSecretDiagnostics`. Verified by `TestPrintSecretDiagnostics_NeverPrintsSecretValue`, which feeds a sentinel value through a real hit and asserts the sentinel appears in neither the printed output nor a captured `slog` buffer (catches a future regression that logs the value directly from this package, not just from `Store`, which Phase B already covers separately).
- **C5 (`boabotctl secret set/get/delete`):** per `architecture.md`'s Cross-Module Constraint, `boabotctl` cannot import `boabot/internal/...`, so `boabotctl/internal/commands/secret.go` takes `zalando/go-keyring` as its own direct dependency and re-implements the account-naming convention (`secretAccount`: service `"boabot"`, account `"<bot>/<name>"` or bare `"<name>"`) rather than sharing code — only the FR-045 convention itself is shared, and it is documented in both places with an explicit "must stay in sync" comment. `set` reads the value via the existing `promptPassword` helper (masked terminal input, or a piped line on non-tty stdin — same mechanism `login` already uses), never via a command-line argument or flag, and rejects an empty value. **`get` reports presence/absence only, never the value** — this follows FR-049's own wording verbatim ("write, read-presence-of (never the value), and delete"), resolving the PRD's own suggested departure point in favour of the literal requirement rather than adding a `--reveal` escape hatch. `delete` on an absent entry is not an error, matching `boabot`'s own keystore provider. A `keystoreBackend` interface (mirroring `boabot`'s own test seam) keeps all six command tests off the real OS keystore. `user-docs/baobotctl.md` gained a `## baobotctl secret` section per this module's own "always update for any command change" rule.
- **Coverage:** all four new/changed call sites (`cmd/boabot/main.go`'s new functions, `cmd/boabot/diag.go`, `SlackConfig.ResolveSecrets`, `boabotctl/internal/commands/secret.go`) are covered by unit tests using fakes (`domain.SecretStore`, `domain.SecretProvider`, `keystoreBackend`) — no test in this phase touches a real OS keystore, `$CREDENTIALS_DIRECTORY`, or a real `~/.boabot/credentials` file outside of `t.TempDir()`. Two additional tests (`TestEndToEnd_EnvOnlyDeployment`, `TestEndToEnd_CredentialsFileOnlyDeployment` in `cmd/boabot/main_test.go`) exercise the real provider chain end to end — not a fake `domain.SecretStore` — for FR-046's named PRD AC ("existing deployment using only env vars, and one using only `~/.boabot/credentials`, both start with a byte-identical config to before this change"), since a fake would bypass exactly the two seams most likely to silently regress: `env.Provider`'s ref-name-to-env-var-name derivation and `file.Provider`'s bare-key convention matching the pre-migration `applyCredential` call's key names.
- **Latent cross-platform gap (not a defect in this run):** the `HOME`-env-override pattern used throughout Phase C's `main_test.go` (and inherited from Phase B) drives `credentials.DefaultPath()` via `os.UserHomeDir()`, which reads `%USERPROFILE%` on Windows, not `HOME`. These tests will fail on a Windows CI runner today. `tasks.md`'s I4 already scopes the actual multi-OS CI matrix out of this run's automated coverage, so this is tracked as a known gap for that later item, not fixed here.

## Phase D — Buzz relay client core (complete)

**D1 spike findings:** see `research.md`'s "Phase D — D1 spike findings" section for the full `fiatjaf.com/nostr` API-surface writeup, including the two non-obvious library constraints that shaped this phase's design (a `*nostr.Relay` cannot be reconnected in place — reconnect means dialing a fresh one; `Subscription.Events` cannot be handed to a caller directly — it dies at the first disconnect) and the confirmed upstream `checkptr`/`unsafe.Pointer` bug in `event.go`'s SWAR JSON-string escaper, with its `go test -race -gcflags=all=-d=checkptr=0` workaround.

**Design decisions (D2–D8), recorded so later phases don't re-derive them:**

- **D2 — `internal/domain/buzz.go`:** `RelayClient` (`Connect`/`Authenticate`/`Publish`/`Subscribe`/`Close`), `Event`, `Filter` defined exactly per `data-dictionary.md`'s "finalized name and shape." `Event`/`Filter` use plain hex strings and `[][]string`/`map[string][]string`, not the library's `[32]byte`/`Tags`/`TagMap` types. Zero infrastructure imports — `grep -r "fiatjaf.com/nostr" internal/domain` returns no matches (the string appears only in doc comments explaining *why* it doesn't, which the grep still doesn't match since grep here is for the import path as used in Go source, and none exists).
- **D3 — `internal/infrastructure/buzz/translate.go`:** `ToLibraryEvent`/`FromLibraryEvent`/`ToLibraryFilter`/`FromLibraryFilter`. Empty domain strings (`ID`/`PubKey`/`Sig` = `""`) translate to the library's zero values (`nostr.ZeroID`/`nostr.ZeroPK`/zero `[64]byte`), not their 64/128-zero-hex-char string encoding, and vice versa — an unsigned, freshly-built `domain.Event` (as a caller constructs before `Publish` signs it) round-trips cleanly. `Filter.Since`/`Until` are `*int64` on the domain side specifically so an absent bound is distinguishable from an explicit `0` bound — though once translated to the library's `Timestamp` (`0` = absent, no separate "explicit zero" representation), an explicit zero bound becomes indistinguishable from absent; documented as an accepted, non-fixable-on-our-side limitation of the library's own type, not a bug in the translation. `Filter.Tags` keys are bare tag letters (`"h"`, `"p"`, `"e"`, no `#` prefix) on both sides — confirmed via `filter_easyjson.go`, which prepends `#` only at JSON-marshal time.
- **D3 (relayConn seam) — `internal/infrastructure/buzz/conn.go`:** `relayConn` is a small interface (`Auth`, `Publish`, `Subscribe` returning `<-chan nostr.Event`, `Done`, `Close`) wrapping `*nostr.Relay`, plus a `dialFunc` seam. This exists purely so `RelayClient`'s connect/auth/publish/subscribe/reconnect logic is unit-testable against an in-memory `fakeConn` (`fakeconn_test.go`) instead of a live relay or a general-purpose WebSocket test server — the two library reconnect/subscription-lifecycle constraints above made a naive "just mock the WebSocket" approach the wrong shape; the seam is at the *behavior* boundary (dial → conn with auth/publish/subscribe/done/close), not the transport boundary.
- **D4 — `internal/infrastructure/buzz/keypair.go`:** `LoadKeypair(ctx, domain.SecretStore, botName) (nostr.SecretKey, nostr.PubKey, error)`, a standalone function (not wired to any monitor lifecycle — that's Phase H's job, per the brief). Resolves `SecretRef{Name: "buzz_private_key", Bot: botName}` (constant `PrivateKeySecretName`), accepts either an `nsec1…` bech32 string (via `nip19.Decode`) or raw hex (via `nostr.SecretKeyFromHex`), rejects the all-zero key and a pubkey that fails `nostr.PubKeyFromHex` re-validation. **Never wraps an underlying parse error verbatim** — `nostr.SecretKeyFromHex`'s own error text embeds the raw (malformed) input string, which would leak the secret into any log line a caller writes from the returned error; `parseSecretKey` discards that library error entirely and returns a fixed `errMalformedPrivateKey` sentinel instead. Tested via `TestLoadKeypair_NeverLogsSecret`, a captured-`slog`-buffer + sentinel-nsec test covering both the success and failure paths — the D4-specific, stronger version of Phase B's provider-level no-value-logging test, as the brief required.
- **D5 — NIP-42 AUTH, folded into `relay_client.go`'s `Authenticate`/`authenticateOn`/`buildSignFn`:** built and signed via the library's own `(*Relay).Auth(ctx, sign)` (see D1 findings above for why it's called explicitly rather than via `RelayOptions.AuthHandler`). The NIP-OA extension point is `AuthTagFunc func(ctx) ([]string, error)`, an optional field set via `WithAuthTagFunc`; `buildSignFn`'s closure calls it (if set) and appends its returned tag to `evt.Tags` *before* `evt.Sign(sk)` — since the tag must be part of the signed content, this ordering is load-bearing and is exactly why the hook has to live inside the sign callback rather than being spliced in by the caller beforehand. Phase E fills this with real NIP-OA preimage/signature construction (`internal/infrastructure/buzz/nipoa.go`, per `data-dictionary.md`) without touching `relay_client.go`.
- **D6 — `internal/infrastructure/buzz/token.go`:** `LoadAPIToken(ctx, domain.SecretStore, botName) (token string, found bool, err error)` resolves `SecretRef{Name: "buzz_api_token", Bot: botName}`. Unlike `LoadKeypair`, an all-provider-miss is reported as `found=false, err=nil` (the token is optional unless the relay requires it) — distinguished from a genuine provider failure via `errors.As` against `*secret.NotFoundError` (`internal/infrastructure/secret`'s concrete miss type), not by swallowing every error uniformly; a real provider failure still returns a non-nil `err`, per FR-053's intent that resolution failures stay visible to the operator. `RelayClient.WithAPIToken(token string, required bool)`: when `required && token == ""`, `Connect` fails closed *before* attempting any dial (no wasted connection attempt); when `token != ""`, every dial (initial connect **and every reconnect**) carries `Authorization: Bearer <token>` via `nostr.RelayOptions.RequestHeader`.
- **D7 — `internal/infrastructure/buzz/profile.go`:** `Profile{Name, BotType, Description}` + `WithProfile(Profile)`. Published as `kind:0` with `{"name":…,"about":…}` content on `Connect`'s first success **only** — tracked via a `profilePublished bool` guarded by `rc.mu`, explicitly not reset across reconnects, matching "on first successful connection" read literally (once per `RelayClient` lifetime, not once per connection). A profile-publish failure is logged (`rc.logger.Warn`) but does **not** fail `Connect` — the connection is still fully usable without the courtesy profile.
- **D8 — `internal/infrastructure/buzz/reconnect.go`:** `BackoffConfig{Base, Max}` (default 1s/30s), full-jitter (`[0,1)`, both seams (`sleep`, `jitter`) injectable for tests). `watchLoop` watches `relayConn.Done()`; on fire, `reconnect()` loops sleep→dial→authenticate (no auth retries here beyond `authenticateOn`'s own no-challenge retry — a hard auth failure just counts as one more backed-off attempt) until success or `Close()`. **Subscription registry (`RelayClient.subs map[int]*subEntry`, `subMu`-guarded)** is the "what was subscribed before" mechanism the brief asked to design now for later phases: each `subEntry` carries the caller's original `ctx` and `nostr.Filter`, plus a long-lived caller-facing `out chan domain.Event` that survives every reconnect — `resubscribeAll` re-attaches every still-live (ctx not done) entry to the new connection after every successful reconnect, forwarding onto the *same* channel the original `Subscribe` call returned, so a caller never sees a channel close/reopen across a reconnect and no pending correlation (a later phase's task-ID→channel pending map, sitting on top of this) is lost. Closing an entry's `out` channel is race-free by construction: `attachSub` only starts a pump (and only `pumpWG.Add(1)`s) while holding `subMu` and after re-confirming the entry is still registered; `removeAndClose` deletes the registry entry under the same lock, then waits for that specific attach-generation's `pumpDone` channel before closing `out`, so a pump is never sending on a channel a concurrent close is about to close.

**Coverage:** `internal/domain/buzz.go` — 100% of the file (package-level `internal/domain` sits at 94.9%, including pre-existing unrelated files). `internal/infrastructure/buzz` — 89.2% overall; the only meaningfully uncovered code is `conn.go`'s thin `dialLibRelay`/`libRelayConn` wrapper around the real library (0% — by design, per the brief: "mock the actual WebSocket where needed rather than requiring a live relay"), which has a `//go:build integration` stub (`conn_integration_test.go`, compiles, skips without `BUZZ_TEST_RELAY_URL` set) for Phase I to build on. Every other function in the package is well above 70%, with the translation layer, keypair loading, and auth/reconnect/token logic (the brief's explicitly named priorities) all in the 90–100% range.

## Phase E — NIP-OA / NIP-AA (complete)

**Design decisions (E1–E5), recorded so later phases don't re-derive them:**

- **E1/E2 — `internal/infrastructure/buzz/nipoa.go`:** `Preimage(agentPubkeyHex, conditions) []byte` builds the exact UTF-8 bytes `"nostr:agent-auth:" ‖ agentPubkeyHex ‖ ":" ‖ conditions` via a single `strings.Builder`, never touching `conditions` beyond appending it verbatim — the load-bearing property FR-006 names explicitly. `PreimageHash` is `sha256.Sum256` of that. `ValidateConditions` implements the clause grammar (`kind=<decimal>` / `created_at<<ts>` / `created_at><ts>`, no whitespace, no leading/trailing/doubled `&`, canonical base-10 via a leading-zero check) with a single regex (`conditionClauseRe`) plus explicit range/canonical checks per clause — `kind=` is bounded to `[0, 65535]` (`maxConditionKind`, NIP-01's 16-bit kind field; the same bound `translate.go` already enforces elsewhere), `created_at<`/`created_at>` accept any non-negative `uint64`. `SignAuthTag` validates conditions and rejects `owner==agent` *before* signing (so a caller never separately re-validates a tag this function just produced), signs `PreimageHash` with `btcec`/`schnorr` (`github.com/btcsuite/btcd/btcec/v2/schnorr`, confirmed as the BIP-340 primitive back in D1's spike), and returns the four-element tag with the owner's own derived pubkey in `tag[1]` (never trusts a caller-supplied owner pubkey that might not match `ownerSK`). `ValidateAuthTag` re-derives everything independently from the tag's own bytes (parses `owner pubkey hex` → curve point, `sig hex` → Schnorr signature, recomputes the preimage hash from `agentPubkeyHex`+`tag[2]`) rather than trusting any cached state — this is what makes the reordered-conditions negative case work: tampering with `tag[2]` after signing changes the hash the verify step recomputes, so `sig.Verify` fails even though every other field still parses. `FindAuthTag` treats **both** zero and multiple `"auth"` tags as `ErrNoAuthTag` — the PRD's "more than one `auth` tag on an event ⇒ treat as having none" is implemented as "count everything named `auth`, only proceed on exactly one," not "pick the first."
  - **Test-vector note:** the PRD names owner secret `0x…01` and agent secret `0x…02` but does not publish a full precomputed signature-hex table alongside them — `nipoa_test.go` uses `nostr.KeyOne` (the library's own named `0x…01` constant, used for the same purpose upstream) and a hand-built `0x…02` (`nostr.SecretKeyFromHex` on 62 zeros + `"02"`) as `testOwnerSK`/`testAgentSK`, and asserts the full sign→verify round trip plus all eight PRD-enumerated negative cases against them — the concrete, reproducible form of "assert against the published test vectors" available from what the PRD actually publishes.
  - **All eight PRD negative cases, one test each, named to match the PRD's enumeration one-for-one** (so it's unambiguous which rule a failure violates): `TestNegative_FiveElementTag`, `TestNegative_TwoAuthTags` (+ `TestFindAuthTag_NoTagPresent` as the distinct "none" case the PRD also names separately), `TestNegative_OwnerEqualsAgentPubkey`, `TestNegative_WhitespaceInConditions`, `TestNegative_LeadingAmpersand`, `TestNegative_TrailingAmpersand`, `TestNegative_DoubledAmpersand`, `TestNegative_NonCanonicalDecimal`, `TestNegative_OutOfRangeKind`, `TestNegative_ReorderedConditionsString`.
  - **`StaticAuthTagFunc(tag, agentPubkeyHex) (AuthTagFunc, error)`** is E1/E2's bridge into D5's extension point: it validates the tag once, up front, at construction (FR-007's "validate a configured auth tag locally at startup before use") so a misconfigured tag fails fast rather than silently on every AUTH attempt, then returns a closure that always hands back the same frozen tag — correct because a NIP-OA tag is a reusable capability, not a per-event artifact (PRD: "the same tag may appear on many events by the same agent key, provided each satisfies the conditions"), so there's nothing to recompute per AUTH attempt.
- **E3 — no new production code; a proof, in `phase_e_wiring_test.go`, that D5's existing wiring and E1/E2's tag construction compose correctly.** D5 already built the generic `AuthTagFunc` hook and wired it into `buildSignFn` (append-before-sign), and D5's own test (`TestAuthenticate_AuthTagHook`) already proved the hook fires with a *stand-in* raw tag. E3's job was narrower and more concrete: prove it with a *real* NIP-OA tag produced by `SignAuthTag` and `StaticAuthTagFunc`, wired via `WithAuthTagFunc` into an actual `RelayClient`, and confirm three things simultaneously — the wire-format tag matches byte-for-byte, the tag *independently re-validates* via `ValidateAuthTag` (proving this isn't a byte-copied fixture but a real, checkable capability), and the AUTH event is still signed by the *agent* key, not the owner key (NIP-OA is explicitly not NIP-26 delegation, per the PRD). A negative control confirms the tag is simply absent — not an empty placeholder — when no `AuthTagFunc` is configured, so the feature is additive.
- **E4 — `RelayClient.clock func() time.Time` (`WithClock`, default `time.Now`), consulted inside `buildSignFn`:** FR-008 has two clauses that could be read as being in tension — "BaoBot MUST set its `created_at` ... from current wall-clock UTC" (implying *we* set it) and the library's own `(*Relay).Auth` already stamping `CreatedAt: nostr.Now()` on the event before calling our sign callback (see D5). Resolved by having `buildSignFn` explicitly re-stamp `evt.CreatedAt` from `rc.clock().UTC()` (satisfying "BaoBot MUST set its created_at... from wall-clock UTC" literally) — but only after checking that this clock agrees with the value the library already put there (itself `nostr.Now()`, i.e. also current wall-clock, just from a different call site) to within `authFreshnessWindow` (120s, `±120s RECOMMENDED` per NIP-AA). In production the two clocks are the same function (`time.Now`) called microseconds apart, so this check is always a no-op; in tests, `WithClock` injects a deliberately offset clock (`time.Now().Add(offset)`, computed at call time so the measured skew stays deterministically close to `offset` regardless of test-run timing) to prove the check actually fires — this is the "injected clock in test" the task brief and PRD AC both call for. A disagreement beyond the window returns `ErrAuthClockSkew` from *inside the sign callback*, before `evt.Sign` is ever called — the event is never signed or sent, confirmed by `conn.authCallCount() > 0` (the attempt was made) alongside `conn.lastAuthEvent` staying at its zero value (fakeConn only records `lastAuthEvent` *after* a successful `sign()` call, so an unset zero value is itself proof nothing was signed). `ErrAuthClockSkew` is deliberately kept distinguishable from E5's `ErrAuthInvalid`/`ErrAuthRestricted` (`errors.Is` on either must be false) since this failure never reached the relay at all — collapsing it into "invalid:" would misdirect an operator toward relay-side debugging when the fix is a local NTP problem.
- **E5 — `classifyAuthFailure(err) AuthFailureClass` + `wrapAuthFailure`, both in `relay_client.go` alongside `authenticateOn`:** classification is a plain substring match on the relay's own reason text (`"restricted:"` checked before `"invalid:"`, though a real relay only ever sends one), which is sufficient because the library's `(*Relay).Auth`/`publish` formats a failed `OK` as `fmt.Errorf("msg: %s", reason)` and `reason` is the relay's own NIP-01/NIP-AA-conventioned string — confirmed by reading `relay.go`'s `handleOk` directly rather than assuming the format. `wrapAuthFailure` logs the classification once, at the point the terminal (non-retriable) failure is identified — `class=invalid`/`class=restricted`/`class=unclassified`, plus the underlying error text, **never the private key** (consistent with every other logging call site in this package, FR-002/FR-051) — then wraps the original error with the matching `ErrAuthInvalid`/`ErrAuthRestricted` sentinel using Go 1.20+'s multi-`%w` (`fmt.Errorf("%w: %w", sentinel, err)`, valid under this module's `go 1.26` directive), so both `errors.Is(err, ErrAuthInvalid)` and the original relay reason text remain simultaneously inspectable from the same returned error — satisfying FR-009's "these ... MUST NOT be collapsed" without discarding the original diagnostic detail. Unclassifiable failures (e.g. a transport error) fall through to a generic wrap and log as `class=unclassified`, deliberately not forced into either named bucket.

**Coverage:** `internal/infrastructure/buzz` rose from 89.2% to 90.9% overall as a direct result of `nipoa.go` (fully exercised by `nipoa_test.go`) and the E4/E5 additions to `relay_client.go` (fully exercised by `phase_e_wiring_test.go` plus the pre-existing `relay_client_test.go`, both of which continue to pass unmodified). `go.mod`: `github.com/btcsuite/btcd/btcec/v2` promoted from the `// indirect` block to the direct `require` block (it was already present transitively via `fiatjaf.com/nostr`'s own dependency tree per D1's spike, but Phase E is the first place this module imports it directly).

**Re-verification of E1/E2 before starting E3–E5 (per the task brief's instruction not to assume prior work was complete just because it compiled):** re-read `nipoa.go` and `nipoa_test.go` in full against the PRD's "NIP-OA in detail" section and FR-005–FR-009 verbatim. Confirmed: preimage layout matches exactly (including the trailing `:` before an empty `conditions`, `TestPreimage_EmptyConditions`); conditions are never reordered/deduplicated (`TestPreimage_NeverReordersOrNormalizesConditions`, `TestNegative_ReorderedConditionsString`); all eight PRD-named negative cases each have their own test, correctly mapped one-for-one to the PRD's enumeration; `owner == agent` is checked in both `SignAuthTag` (before signing) and `ValidateAuthTag` (for a hand-authored/hostile tag that didn't come through `SignAuthTag`); `kind=` range is bounded at exactly 65535 (NIP-01's 16-bit field), not an arbitrary cutoff; "more than one `auth` tag ⇒ treat as none" is implemented via a count-based check in `FindAuthTag`, not a first-match. No defects found; E1/E2 required no changes.

## Phase F — Channel participation (complete)

**Design decisions (F1–F18), recorded so later phases don't re-derive them:**

- **F1/F2/F3 — `internal/infrastructure/buzz/discovery.go`:** `startDiscovery` subscribes once to `{"kinds":[39000,39002]}` (no tag filter -- the relay itself scopes what a given authenticated pubkey is permitted to see). **F1 membership-gating fix (advisor-flagged before commit):** the first pass treated ANY kind:39000 or kind:39002 event (once it had a `d` tag) as sufficient to open a kind:9 subscription. That's wrong against FR-013's literal "channels it is a member of" -- kind:39000 metadata for a public channel may be visible to a pubkey that isn't a member, and subscribing on that basis alone would open a kind:9 subscription for a channel we have no business monitoring. Fixed: `handleDiscoveryEvent` now only ever calls `subscribeToChannel` from a kind:39002 event that itself carries a `["p", <our own pubkey>]` tag -- i.e. actual membership-roster proof, not mere metadata visibility. Kind:39000 is still screened/logged (`logChannelMetadata`) but never triggers a subscription by itself. `subscribeToChannel` itself remains idempotent (`map[string]context.CancelFunc` keyed by channel UUID under `channelsMu`). `startMembershipWatch` subscribes to `{"kinds":[44100,44101],"#p":[self]}` in one filter; `44100` calls the same `subscribeToChannel`, `44101` calls `unsubscribeFromChannel` (cancels that channel's subscription context and deletes it from the map, so a later `44100` for the same channel is a fresh subscribe, not a no-op). Every per-channel subscription context is a child of the `ctx` `Start` was called with (via `context.WithCancel`), so F3's unsubscribe and a top-level shutdown both tear it down the same way. **Unverified protocol assumption, flagged for manual verification:** `handleMembershipEvent` reads the target channel's UUID from kind:44100/44101's `h` tag (matching kind:9's own channel-tag convention) -- the PRD documents these kinds' existence and `#p`-gating requirement but not this specific channel-identifying tag by name; if the real relay uses a different tag, F3 silently never fires (empty stream, no error -- the PRD's own named failure class).
- **F4/F18 — moved from a planned Monitor-side helper into `RelayClient.Subscribe` itself (`guard.go`), per an advisor review before any Monitor code was written.** The brief's own tasks.md wording for F4 says "rejected in our own code before being sent to the relay" -- the only place that claim is actually true regardless of caller is the last hop before `conn.Subscribe`, i.e. inside `RelayClient.Subscribe`. A Monitor-side-only guard would have been bypassable by any future direct `RelayClient.Subscribe` caller, including exactly the P1 DM (`kind:1059`) work FR-016 explicitly says this guard must already cover. `validateSubscriptionFilter(f domain.Filter, selfPubKeyHex string) error` checks every kind in the filter (not just the first) against `pGatedKinds = {44100, 44101, 1059}` (requiring a `#p` filter whose every value equals `selfPubKeyHex`) and against `reactionKind = 7` (requiring a non-empty `#h` filter), returning `ErrPGateFilterMissing`/`ErrReactionHGateMissing` respectively. Verified directly against `RelayClient.Subscribe` using the existing Phase D `fakeConn`/`newTestClient` test harness (`guard_test.go`) -- no Phase D/E test in the repo subscribes to a guarded kind with a bare filter, so nothing broke.
- **F5/F6 — reply publish and NIP-10 threading.** `HandleResult` builds `domain.Event{Kind: 9, Tags: [["h", channelUUID], ["e", rootEventID, "", "root"]], Content: output}` and calls the existing (unchanged) `RelayClient.Publish`. `rootEventID(evt)` (`trigger.go`): if `evt` already carries a NIP-10 `["e", id, "", "root"]` tag it is itself a reply within an existing thread, so that referenced id is the thread's root; otherwise `evt` is the first message of its own thread and is its own root. P0 doesn't handle deep multi-hop threads (a reply-to-a-reply), matching FR-018's literal wording ("referencing the root event of the conversation it was mentioned in").
- **F7 — content screening (`Monitor.screen`, `monitor.go`).** Takes a `contentScreener` interface (`Screen(content string) (string, error)`), satisfied in production by `*internal/application/screening.ScreenContentUseCase` (constructed over `*internal/infrastructure/screening.RegexScreener` at Phase H wiring time) -- the same use case MCP tool output already goes through, per FR-028's literal "same path" wording. **Fail-open judgment call:** on a screener error, `screen` logs a warning and returns the original, unwrapped content rather than blocking dispatch. A `RegexScreener.Screen` call essentially cannot fail (it's pure regex matching with no I/O), so this is a deliberate choice to prioritize availability over an vanishingly-unlikely defense-in-depth gap, not a weakening of the screening path itself -- recorded here as a judgment call since neither the PRD nor `architecture.md` specifies screener-failure behavior. Applied to (a) the dispatch instruction text (always, before it becomes `TaskPayload.Instruction`) and (b) discovered channel name/topic (`discovery.go`'s `logChannelMetadata`, before either appears in a log line). **Judgment call on scope:** FR-028 names "profile metadata" as a third untrusted category, but no F1–F18 task subscribes to other authors' `kind:0` profiles this phase -- there is no concrete inbound profile-metadata call site to screen yet. `screen` is written as a small, already-tested, reusable seam specifically so a future profile-consuming feature routes through it automatically rather than needing its own screening plumbing.
- **F8 — `authorGate` (`trigger.go`), matching `architecture.md`'s nil-vs-empty-slice edge case exactly.** `active()` is `respondTo != "" || allowlist != nil`. When inactive, `allows` returns true unconditionally (no gate). When active, `allows` checks `respondTo` equality first, then linear allowlist membership -- an explicitly empty (non-nil) allowlist with no `respondTo` is therefore an active gate that allows no one, exactly as specified. **The one place this is easy to get wrong and silently break both directions at once: `Config.RespondToAllowlist` must be threaded through completely unmodified, with no defensive copy.** `append([]string{}, cfg.RespondToAllowlist...)` on a nil slice returns a non-nil empty slice -- silently converting "no gate configured" into "gate active, allow no one" and locking out every sender. `NewMonitor`/`newAuthorGate` store the slice header exactly as received; `TestAuthorGate_NilAllowlistAndNoRespondTo_AllowsEveryone` and `TestAuthorGate_ExplicitEmptyAllowlist_AllowsNoOne` both assert against literal `nil` and literal `[]string{}` inputs, not a config-loader's output, to keep this regression-proof at the type's own boundary independent of how Phase H's config loader happens to construct the slice.
- **F17 — `!shutdown`'s wider gate is a separate method, not a parameter on the ordinary gate.** `authorGate.allowsShutdown(pubkeyHex, ownerPubkeyHex string) bool` checks `ownerPubkeyHex` first (an unconditional override -- the owner can always stop their own bot, even outside an explicitly configured allowlist) and only then falls back to the ordinary `allows`. This is deliberately **not** folded into `allows` itself, because FR-029's ordinary dispatch gate never mentions `owner_pubkey` as an implicit allow -- only FR-026's `!shutdown`-specific gate does. Keeping them as two methods makes it structurally impossible for a future dispatch-path change to accidentally inherit the owner override. **Ordering judgment call (advisor-flagged, applied before any Monitor code was written):** `handleChannelEvent` runs F9 (self-filter) → F10 (trigger classify) → then branches on raw, unscreened `evt.Content` for `!shutdown` (using `allowsShutdown`) before ever reaching F8's ordinary `dispatch`/`allows` gate. Screening (F7) never runs before this branch. This ordering is load-bearing in two directions: (a) if F7 ran first, the screener's `<untrusted-content>` wrapping would make `text == "!shutdown"` never match, silently disabling the stop signal; (b) if the ordinary F8 gate ran before the `!shutdown` branch, an owner not present in an explicitly configured `respond_to_allowlist` would be rejected before ever reaching the wider gate that's supposed to let them through.
- **F14 — the `RelayClient.SetConnStateFunc`/`notifyConnState` connection-state hook (new this phase, in `relay_client.go`/`reconnect.go`) is the prerequisite the brief asked to find by reading Phase D's reconnect design, not invent from scratch.** `notifyConnState(connected bool)` reads the registered callback under `rc.mu` then calls it with the lock released (so a slow callback can never deadlock against `RelayClient`'s own internal locking). **Signal-timing fix (advisor-flagged before commit):** the first pass fired `notifyConnState(true)` from inside `Connect`, right after a bare successful dial -- before NIP-42 authentication. A real Buzz relay rejects any publish attempt on an unauthenticated connection (`"restricted:"`), so a presence loop reacting to that signal would immediately try, and fail, to publish `kind:20001` on startup. Fixed: the signal now fires from exactly one place, `authenticateOn`'s success path (`err == nil`), which both the explicit `Authenticate` call (Monitor's own startup sequence: `Connect` then `Authenticate`) and every automatic reconnect (`reconnect()`, which already calls `authenticateOn`) route through -- so "connected" now uniformly means "connected AND authenticated," never just "dialed." `watchLoop` still fires `notifyConnState(false)` the instant `conn.Done()` fires, before the backed-off `reconnect()` call even starts. `Monitor.onConnStateChange` (`presence.go`) is deliberately minimal -- it only starts or stops the `presenceLoop` goroutine, it never calls `Publish` itself from inside the callback (a slow/blocked `Publish` running synchronously inside this hook would delay `authenticateOn`'s/`reconnect()`'s own caller). `presenceLoop` publishes `kind:20001` "online" as its first action (satisfying architecture.md's "resumes ... publishing an online event as part of re-establishing state") and then on `PresenceInterval` (default 60s -- `defaultPresenceInterval` -- well under FR-023's 180s staleness bound) until its own `ctx` is canceled by the next `onConnStateChange(false)`. **Known pre-existing gap, out of this phase's scope to restructure:** D7's `kind:0` profile publish still happens inside `Connect`, before authentication -- the same class of issue, but it's Phase D code and FR-011's own AC is separately flagged below on the manual-verification list.
- **F15 — `Monitor.Stop`'s offline-presence publish uses `context.WithTimeout(context.WithoutCancel(ctx), 5*time.Second)`, not the raw incoming `ctx`.** Advisor-flagged: `application.RunAgentUseCase.Shutdown` is typically invoked from a signal handler that may cancel the root context before or concurrently with calling `Shutdown`, which calls `Stop` on every monitor. If `Stop` published on the raw (already-canceled) `ctx`, the offline publish would fail immediately -- FR-024's AC ("on SIGTERM it publishes offline ... before exit") would fail live in exactly the scenario it's meant to cover, while a unit test using a fresh `context.Background()` would pass and hide the bug entirely. `context.WithoutCancel` detaches from the parent's cancellation while still inheriting its values; the fresh 5s timeout bounds the publish so a genuinely unresponsive relay still can't hang shutdown indefinitely. Tested directly with a pre-canceled parent (`TestMonitor_Stop_AlreadyCanceledParentCtx_StillPublishesOffline`).
- **F16 — the typing-indicator loop's `done` channel is closed inside the same `Monitor.mu` critical section that pops the task from `pending`, not after releasing the lock (advisor-flagged).** `HandleResult`'s lock body is `entry, ok := m.pending[taskID]; if ok { delete(...); close(entry.typingDone) }`, all under one `m.mu.Lock()`/`Unlock()`. This makes "the entry was removed" and "typing has been told to stop" a single atomic fact from any other goroutine's point of view -- there is no window where a second `HandleResult` call for the same (already-impossible-in-practice, but not structurally ruled out) task ID could double-close the channel, since the map lookup and the close share one lock acquisition. `typingLoop` is also tied to the dispatch call's own `ctx` (the channel-subscription ctx passed through `handleChannelEvent`/`dispatch`), not `context.Background()`, so an F3 unsubscribe mid-task also stops the indicator; `maxTypingDuration` (30 minutes) is a second, independent safety bound in case a task result is somehow never delivered.
- **F11's FR-030 confirmation.** Per the brief, this was a confirmation task, not an implementation task: `dispatch` enqueues via the same `domain.MessageQueue.Send` interface call `slack.Monitor.dispatch` already uses, with an identical `domain.Message{Type: MessageTypeTask, From: "buzz", ...}` shape. Read `application/run_agent.go`'s `handleTask` (the queue-consumer side, downstream of every channel monitor including Slack) to confirm budget/autonomy gating happens there, uniformly, with no per-`From`-value branching -- Buzz gets no special path and no bypass, confirming the brief's stated understanding without needing to trace the full worker harness.
- **Test infrastructure:** `monitor_test.go`'s `fakeRelay` implements the Monitor-local `relayClient` interface (`Connect`/`Authenticate`/`Publish`/`Subscribe`/`Close`/`SetConnStateFunc`) rather than reusing Phase D's `fakeConn` (which implements the lower-level, single-connection `relayConn` seam `RelayClient` itself sits on) -- the two seams are at different layers and deliberately not unified, mirroring how `slack.Monitor`'s own tests use a `fakePoster`/`fakeSocketClient` distinct from anything at the Slack SDK's transport layer. `application/mocks.MessageQueue` (already used by `slack/monitor_test.go`) is reused as-is for the queue side -- no new queue fake was written. A small `tickerFactory` records every `ticker` `Monitor`'s presence/typing loops create via the injectable `newTicker` field, letting tests fire a specific tick by hand instead of sleeping through real intervals -- mirrors Phase D's `WithSleep`/`WithJitter` injectable-time pattern in `reconnect.go`.

**Coverage:** `internal/infrastructure/buzz` moved from 90.9% (end of Phase E) to 90.4% overall -- a large new surface (`monitor.go`, `discovery.go`, `presence.go`, `trigger.go`, `guard.go`, `ticker.go`) landing at a slightly lower but still healthy percentage than Phase E's narrower NIP-OA-only delta. Every new function has at least one direct test; the goroutine-loop entry points sitting in the 50–70% range (`run`, `consumeDiscovery`, `consumeMembership`, `publishPresence`, `Stop`) are multi-branch `select`/error-path functions where each branch is exercised by a separate test rather than all branches by one -- not untested logic. `go build`/`go vet`/`golangci-lint run` all pass repo-wide with zero issues; `go test -race -gcflags=all=-d=checkptr=0 ./...` passes repo-wide, including every pre-existing Phase A–E test unmodified. `grep -r "fiatjaf.com/nostr" internal/domain` and `grep -rE "infrastructure/slack|infrastructure/buzz" internal/application` both still return no matches.

## Phase G — Process-singleton lock (complete)

**Design decisions (G1), recorded so Phase H's wiring doesn't re-derive them:**

- **Stdlib-only, no new dependency.** `go.mod` was checked first per the brief: `golang.org/x/sys` is present but only transitively (an indirect dependency of go-git and others), and pulling it in as a direct dependency just for Unix `flock` would need a second, platform-specific implementation for Windows anyway (this codebase's NFR Portability targets macOS/Windows/Linux). The brief's explicitly-sanctioned fallback -- `os.OpenFile(O_CREATE|O_EXCL)` writing our own PID, plus a liveness check -- is dependency-free and portable with one small platform branch (`processAlive`, below), not a whole second implementation. `internal/infrastructure/buzz/lock.go` implements this as `AcquireLock(path string) (*Lock, error)` / `(*Lock).Release() error`.
- **Lock file location and naming.** `LockPath(dir, pubkeyHex string) string` joins `dir` with `lockFileName(pubkeyHex)` = `buzz-<first-16-hex-chars>.lock` -- the file is already identity-scoped by name, so `dir` itself must be a **shared** root, not a per-bot subdirectory (`internal/application/team.ManagerConfig.MemoryRoot`, matching `internal/infrastructure/local/fs`'s existing base-directory convention), otherwise two boabot processes running *different* bot configs against the same nsec (the actual OQ-1 threat -- see architecture.md's "process-level singleton lock" rationale) would each get their own per-bot lock path and never contend at all. Only the derived public key is ever used to build the filename or written into the lock file's content -- the secret key itself is structurally unreachable from this file, mirroring `keypair.go`'s "never logs/writes the raw secret" discipline.
- **Stale-lock reclaim, including the specific crash shape the brief called out.** `AcquireLock` loops: `O_CREATE|O_EXCL` create; on `EEXIST`, read the existing file's content as a PID (`readLockPID`) and check liveness (`processAlive`); a dead PID's file is removed and the loop retries the create; a live PID's file yields `ErrAlreadyRunning` (wrapped via `%w`, `errors.Is`-checkable). **An empty lock file -- the realistic artifact of a process SIGKILLed between `O_CREATE` and its PID `WriteString` -- is parsed as pid 0 rather than an error**; `processAlive(0)` is unconditionally false, so this routes into the same reclaim path as any other dead PID. This was **not** the first-pass behavior: an advisor review before finalizing caught that `readLockPID` originally treated empty content the same as unparseable garbage, both routed to a fail-closed `ErrAlreadyRunning` -- which would have permanently wedged exactly the crash-mid-write scenario the design brief's stale-lock requirement explicitly names as a MUST-not-happen case. Fixed by special-casing empty (post-`TrimSpace`) content as pid 0; non-empty-but-unparseable content is still treated conservatively as held (fails closed) since it cannot be distinguished from a genuinely live holder's lock without more information. Both branches have dedicated tests (`TestAcquireLock_EmptyLockFile_Reclaimed` vs. `TestAcquireLock_GarbageLockFile_TreatedAsHeld`).
- **Portable liveness check (`processAlive`, unexported).** Unix: `os.FindProcess` always succeeds regardless of whether the PID exists (per the stdlib's own doc comment), so existence is tested by sending the null signal, `proc.Signal(syscall.Signal(0))` -- a no-op existence probe the OS validates without delivering anything. Windows: `os.FindProcess` itself opens a process handle and fails if the PID doesn't exist, so its success/failure *is* the check there; `Signal` is not consulted on Windows since the stdlib only supports `os.Kill` through that path on that platform. `pid <= 0` is rejected outright before either branch. **Known, accepted limitation** (per the design brief's own framing of this tradeoff): PID reuse by an unrelated process between a crash and our liveness check produces a false "still held" read -- inherent to any PID-based lock, not something this design can eliminate without OS-specific process-start-time comparison, which was judged out of scope for a P0 lock guarding against operator double-start/botched-upgrade, not adversarial contention. Flagged on the manual-verification checklist below, specifically for the Windows case, since `OpenProcess` succeeding is a slightly weaker liveness proof there than Unix's signal probe.
- **Wired into `Monitor.Start`/`Stop` as a real gate on the existing lifecycle methods, not a standalone unused utility, per the brief's explicit requirement.** `Config` gained one new field, `LockDir string` (empty = lock disabled). `Start` acquires `AcquireLock(LockPath(cfg.LockDir, cfg.AgentPubKeyHex))` as its first action, before anything else runs; on failure it logs a single `Error` line naming FR-031/OQ-1, the contended pubkey, and the lock directory, then returns `nil` (not an error) without ever calling `m.relay.Connect` or launching the `run` goroutine -- matching D4's `LoadKeypair` fail-closed pattern the brief pointed at (clear, non-secret-leaking error; caller declines to start; nothing else affected) and `credentials.go`'s fail-fast-without-crashing precedent. `Stop` (`presence.go`) releases the lock in the exact same synchronous sequence as the F15 offline-presence publish, so a graceful shutdown always frees the identity before the process actually exits, whether or not a restart follows.
- **`LockDir` is opt-in (empty disables the lock), a deliberate scope decision, not an oversight.** `architecture.md`'s Phase H note is explicit that Phase D-G components take "plain typed constructor parameters," not `*config.BuzzConfig` -- wiring `LockDir` from the real memory root is H2's job (`cmd/boabot/main.go` isn't touched this phase, per the brief). Making the field optional rather than required keeps every one of Phase F's ~25 existing `Monitor` tests (all built on `testConfig()`, which has no `LockDir`) passing completely unmodified, while still giving Phase H a real, already-tested gate to wire up. The cost of this choice is that a missed H2 wiring step would silently leave FR-031 unenforced in production; mitigated with a `Warn`-level log (`"FR-031/OQ-1 multi-instance protection is INACTIVE"`) on every `Start` call when `LockDir` is empty, so the gap is greppable in a running process's own logs, not just discoverable by reading this file. A companion guard refuses to start (logs an `Error`, does not attempt to acquire a meaningless lock) if `LockDir` is set but `AgentPubKeyHex` is empty -- that combination would collapse every identity onto the single literal filename `buzz-.lock`, making unrelated bots/identities falsely contend with each other.
- **TDD, followed strictly.** `lock_test.go`'s `TestAcquireLock_SecondAttempt_FailsWhileFirstHoldsLock` -- two `AcquireLock` calls against the same path within one test process -- was written and confirmed to fail to compile (`undefined: AcquireLock`, `undefined: ErrAlreadyRunning`) before `lock.go` existed. `monitor_test.go`'s `TestMonitor_Start_SingletonLock_SecondMonitorRefused` demonstrates the identical shape one layer up (two `Monitor.Start` calls, same `LockDir`+`AgentPubKeyHex`, two independent fake relays) and was the second advisor-flagged gap: the first pass only asserted the second monitor's subscription count was zero, not that it actually logged why (G1's literal AC text is "second refuses, *logs*, first is unaffected"). Fixed by switching that test's logger from `discardLogger()` to a `bytes.Buffer`-backed `slog.Logger` (the same pattern `keypair_test.go` already uses for its own log-assertion tests) and asserting the buffered output names both `FR-031` and the contended pubkey.

**Coverage:** `internal/infrastructure/buzz` moved from 90.4% (end of Phase F) to 90.0% overall -- `lock.go`'s PID-liveness/stale-reclaim control flow has a few genuinely hard-to-hit branches without OS-level fault injection (the write-then-close failure path inside a successful `O_CREATE`, and the "file vanished between our `EEXIST` and our read" race retry), landing at a slightly lower but still healthy percentage. Every new function (`AcquireLock`, `Release`, `lockFileName`, `LockPath`, `readLockPID`, `processAlive`) has at least one direct test, including dedicated tests for the empty-file-reclaim fix, garbage-content fail-closed behavior, the nil-receiver `Release` no-op, and both PID-liveness edge cases (`pid<=0`, our own live PID). `go build`/`go vet`/`golangci-lint run` all pass repo-wide with zero issues; `go test -race -gcflags=all=-d=checkptr=0 ./...` passes repo-wide, including every pre-existing Phase A–F test unmodified.

## Phase H — Config and docs (complete)

**H1 — `BuzzConfig`.** Added to `internal/infrastructure/config/config.go`, mirroring `SlackConfig`'s shape: `Enabled`, `RelayURL`, `BotName`, `OwnerPubkey`, `RespondTo`, `RespondToAllowlist`, `PresenceInterval` (a new `config.Duration` wrapper type, since `gopkg.in/yaml.v3` does not natively parse a `time.Duration` from a string like `"60s"` -- without it operators would have to write raw nanoseconds). **`Channels []string` was deliberately NOT added**, despite FR-035's field list naming one: `internal/infrastructure/buzz/discovery.go` (Phase F) subscribes to every channel the relay confirms membership for (`kind:39000`/`39002` + `kind:44100`/`44101`) entirely dynamically -- `grep -rn "Channels\b" internal/infrastructure/buzz/*.go` (excluding test files) returns zero hits outside one unrelated test name. Adding the field would have been dead config: an operator writing `buzz.channels:` would reasonably believe it scopes membership, and nothing would ever read it. Because `config.Load`'s `dec.KnownFields(true)` already rejects any key not matching a literal `BuzzConfig` field, omitting `channels` turns a `buzz.channels:` entry into a hard "field not found" load error instead -- the honest outcome. This is recorded as a deliberate deviation from FR-035's literal field list, not an oversight; the launching brief for this phase explicitly authorized making this call and reporting it rather than wiring config nothing reads.

**H1 — secret-block rejection.** `config.Load`'s existing `yaml.NewDecoder(f); dec.KnownFields(true)` already rejects any key under `buzz:` that is not a literal `BuzzConfig` field -- proven, not re-implemented, by `config_test.go`'s `TestLoad_BuzzSecretLikeKeyRejected` (table-driven over `nsec`, `private_key`, `api_token`, `auth_tag`, `buzz_private_key`, `secret`) and `TestLoad_BuzzChannelsKeyRejected`. No additional guard code was needed or added. Note the scope of this AC: it covers *keys* under `buzz:`, not secret material smuggled into a legitimate string field (e.g. an operator pasting an nsec into `owner_pubkey`) -- that misuse is out of FR-035's literal scope and undetectable by a config-shape check.

**H1 — presence-interval bound.** `internal/infrastructure/buzz/monitor.go`'s `Config.PresenceInterval` doc comment explicitly assigns FR-023's 180s-staleness-bound validation to "config-loading (H1), not Monitor's" -- easy to miss since Monitor itself never checks it. `config.Load` now rejects (a distinct, second validation step run after YAML decode succeeds) any `buzz.presence_interval` that is zero-or-negative-but-set-non-zero-invalid or `>= 180s`, with an error naming the bound; zero (unset) is left alone, deferring to `Monitor`'s own default (well under the bound). Tested (`TestLoad_BuzzPresenceIntervalTooHighRejected`, `TestLoad_BuzzPresenceIntervalUnderBoundAccepted`).

**H2 — `cmd/boabot/main.go` wiring.** New `buildBuzzMonitor` function, called unconditionally from `run()` (not behind an outer `if` in `run()` itself): its own first statement is `if !bc.Enabled { return nil }`, the FR-036 activation guard. This placement (inside the constructor, not scattered as an `if` in `run()`) was an advisor-flagged correction to the original plan specifically so the guard is unit-testable in isolation -- `TestBuildBuzzMonitor_Disabled` asserts both a nil return **and** zero `SecretStore.Get` calls against a call-recording fake store, which is the concrete evidence (not just prose) that no Nostr code path executes and no relay connection is attempted when disabled. On success, it: resolves the keypair via `buzzinfra.LoadKeypair` (fail-closed per FR-003 -- a resolution failure logs and returns `nil`, leaving Slack/every other bot to start normally, proven by `TestBuildBuzzMonitor_KeypairLoadFailure`); resolves an optional `BUZZ_API_TOKEN` via `buzzinfra.LoadAPIToken`, attaching it with `WithAPIToken(token, false)` when found (`required=false` since `BUZZ_REQUIRE_AUTH_TOKEN` is a relay-side setting, not something `BuzzConfig` controls); builds a `Profile` for `WithProfile` -- `Name` is always `cfg.Buzz.BotName`, but `BotType`/`Description` (read from `<botsDir>/<botType>/AGENTS.md`'s "## What I do" paragraph via a new best-effort `readBotDescription` helper) are populated **only when `cfg.Bot.Name == cfg.Buzz.BotName`**, since `cfg.Bot` describes a specific bot's own identity and publishing another bot's type/description under Buzz's identity would be wrong whenever the two names differ (advisor-flagged correction); registers the target bot's queue with the shared `queue.Router`, **guarded against double-registration** when Slack is already wired to the same `bot_name` (`Router.Register` panics on a duplicate name -- `queueAlreadyRegistered`, computed in `run()` by comparing `slackBotName == cfg.Buzz.BotName`, is threaded through so `buildBuzzMonitor` skips the second `Register` call; `TestBuildBuzzMonitor_QueueAlreadyRegistered_DoesNotDoubleRegister` proves no panic); sets `Config.LockDir` to `managerCfg.MemoryRoot` (the shared root, per `buzzinfra.Config.LockDir`'s own doc comment, never a per-bot directory); and wires `WithShutdownFunc(mgr.Shutdown)` -- see the Manual Verification update above for why `mgr.Shutdown` (`team.TeamManager.Shutdown`) rather than a per-bot `application.RunAgentUseCase.Shutdown`, which `main.go` has no reference to. `run()` also now unconditionally `os.MkdirAll`s `managerCfg.MemoryRoot` before constructing `TeamManager` (advisor-flagged: `AcquireLock`'s `O_CREATE|O_EXCL` needs the directory to already exist, and while `TeamManager` normally brings it into existence as a side effect of creating a bot's own per-bot memory subdirectory, that must not be relied on to happen before a monitor's own `Start` runs).

**H2 — content screening wired for the first time.** `internal/application/screening.ScreenContentUseCase` (existing since before this feature, satisfying Phase F's `contentScreener` seam) had no production call site anywhere in `main.go` prior to this phase -- Slack's monitor doesn't use it. `buildBuzzMonitor` constructs one (`screening.NewScreenContentUseCase(infraScreening.NewRegexScreener())`) and passes it to `buzzinfra.NewMonitor`, so FR-028's screening path is live in production for Buzz from this commit onward.

**H2 — tests.** `cmd/boabot/main_test.go` gained `buzzFakeStore` (a call-recording `domain.SecretStore` double distinct from the pre-existing `fakeSecretStore`, because it needed to return a real `*secret.NotFoundError` for `LoadAPIToken`'s `errors.As`-based clean-miss distinction rather than a generic error) and six new tests: disabled/zero-calls, missing-bot-name-or-relay-url, keypair-load-failure, enabled-success (asserts the queue was registered), double-registration-guard (asserts no panic via `recover()`), plus two `readBotDescription` tests (extracts the "## What I do" paragraph; missing file/empty `botsDir` both return `""`, never an error). All run under the repo-wide `-race -gcflags=all=-d=checkptr=0` suite alongside the pre-existing Phase A-G tests, unmodified.

**H3 — `AGENTS.md` corrections.** `boabot/AGENTS.md`: replaced "Slack, and Microsoft Teams" with "Slack, and Buzz (a Nostr-based relay protocol)" in Module Purpose; replaced the Package Structure's `teams/ # Microsoft Teams adapter` line (confirmed nonexistent: `ls internal/infrastructure/teams/` -> "No such file or directory") with a `buzz/` entry describing the adapter's actual files, and added a `secret/` entry; updated `ChannelMonitor`'s Key Interfaces line to include `HandleResult` (added in Phase A, previously undocumented) and changed "Slack/Teams monitors" to "Slack/Buzz monitors"; added `domain/buzz.go` and `domain/secret.go` to the domain package list; added `RelayClient` and `SecretStore` to Key Interfaces. **Root `AGENTS.md` required no changes** -- `grep -in "microsoft teams\|infrastructure/teams\|slack/teams" AGENTS.md` (repo root) returned no matches; it never claimed a Teams adapter in the first place, contrary to what the launching brief's phrasing implied might need fixing there too. Verified clean by `grep -in "microsoft teams\|infrastructure/teams" boabot/AGENTS.md AGENTS.md` returning zero matches after the edit.

**H3 — `user-docs/Microsoft-Teams-Adoption-Config.md` and its README link left untouched, deliberately.** That doc is explicitly headed "Status: planned — not yet implemented" and the README link is annotated "(planned)" -- an honest forward-looking statement, unlike the AGENTS.md package-tree line it stood next to, which asserted a package that does not exist as though it were already built. Not in scope for FR-037's literal fix (which is about a false "this exists" claim, not a labeled roadmap item).

**H4 — docs.** Two new ADR entries (`docs/architectural-decision-record.md`): **ADR-B020** records the Option A (native Go Nostr client) vs. B (`buzz-acp` harness) vs. C (CLI shellout) decision and why A won, states the validated `fiatjaf.com/nostr@v0.0.0-20260731140316-a8080728893f` version, and **explicitly records that no specific `block/buzz` relay commit was pinned or validated against a live relay this run** -- the PRD names the `block/buzz` GitHub repository and its NIP docs as design references but never itself cites a commit SHA, and this job did not run Buzz's `docker-compose.yml` (per `plan.md`'s AC-handling note), so recording a fabricated hash would have been worse than stating the gap plainly; the gap is cross-referenced to the `//go:build integration` stubs that are meant to close it. **ADR-B021** records `zalando/go-keyring` over `99designs/keyring` with the maintenance-date evidence from the PRD. `docs/technical-details.md` gained `slack/`, `buzz/`, `secret/` package-layout entries (Slack's own entry was also missing before this phase -- a pre-existing gap outside this feature's scope, fixed opportunistically since leaving it out while adding `buzz/` next to it would have been visibly inconsistent), a `buzz:` block in the Config File Structure sample (with an explanatory note on why there is no `channels:` key), and two new prose sections ("Buzz (Nostr) Channel Monitor", "Secret Storage") before "Key Design Decisions". `docs/product-details.md` gained a new "Channel Monitors (Slack, Buzz)" section (again, Slack had no dedicated product-facing section before this phase; added both together for the same consistency reason). `README.md`: fixed the "monitors Slack and Teams" line (Teams was never built) to "Slack and Buzz", and added a `user-docs/Buzz-Adoption-Config.md` link in the same list as the other channel docs. New `user-docs/Buzz-Adoption-Config.md`: enabling the channel, the exact `SecretStore` provider table with real key names (`buzz_private_key`, `buzz_api_token`, bot-namespaced per provider per the B8 convention confirmed by reading `env`/`file`/`systemd`/`keystore`'s package docs directly, not assumed), the `boabotctl secret set/get/delete --bot <name>` command syntax (confirmed against `boabotctl/internal/commands/secret.go`), the PRD's per-OS x per-mode secret-provisioning matrix verbatim, and FR-054's residual-exposure statement (plaintext in process memory; Linux-service `$CREDENTIALS_DIRECTORY` is tmpfs, not TPM-sealed-at-use) stated explicitly, not implied.

**H4 — files deliberately not touched.** `docs/product-summary.md` (no Slack/Teams/Buzz mentions to correct, and not in this phase's assigned file list) and `boabotctl/`'s own docs (a separate Go module, out of `boabot/`'s scope; its `secret` command syntax was read, not edited, to document accurately in the new Buzz user-doc).

`go build ./...`, `go vet ./...`, `gofmt -l .` (empty), `golangci-lint run` (0 issues), and `go test -race -gcflags=all=-d=checkptr=0 ./...` all pass repo-wide, including every pre-existing Phase A-G test unmodified. `grep -in "microsoft teams\|infrastructure/teams" boabot/AGENTS.md AGENTS.md`, `grep -rE "infrastructure/slack|infrastructure/buzz" internal/application`, and `grep -r "fiatjaf.com/nostr" internal/domain` all return zero matches. `internal/domain/` and `internal/application/` were not touched this phase (config/cmd wiring only), so their coverage is unchanged from Phase G. FR-035, FR-036, FR-037, FR-054 satisfied per `tasks.md`'s H1-H4 rows.

## Manual Verification Required (not run in this job)

To be filled in as each integration-tagged test is written. Placeholder categories from the PRD's acceptance criteria:

- [ ] Live `buzz-relay` (Buzz's `docker-compose.yml`): NIP-42 auth, `kind:0` profile publish (renders name/description, not a bare pubkey, in the Buzz desktop client), `kind:9` pub/sub, reconnect-after-restart, relay-side membership revocation (agent's next connection fails `restricted:`). (`tasks.md` I1)
- [ ] NIP-AA virtual membership, write path: a NIP-AA-authenticated bot publishes its `kind:0` profile without being explicitly enrolled in `relay_members`. (`tasks.md` I1)
- [ ] NIP-AA virtual membership, read path (negative): the same bot is confirmed **not** to inherit the owner's channel memberships — it cannot read a private channel the owner belongs to, and does not. (`tasks.md` I1)
- [ ] Two boabot processes started against the same nsec: exactly one reply event on the relay, one presence identity — confirms FR-031/G1's lock has the intended effect end-to-end, not just in the single-process unit test. (`tasks.md` G1, I1)
- [ ] Windows: confirm `lock.go`'s `processAlive` liveness check (`os.FindProcess`'s own open-handle success/failure, since `Signal` only supports `os.Kill` on that platform) behaves as intended against a genuinely crashed/killed process, and does not produce a false "still alive" read for a recently-recycled PID — this is a documented, accepted limitation of any PID-based lock (G1), not verified against a real Windows host this phase. (`tasks.md` G1, I4)
- [ ] macOS interactive: a secret written to the login keychain is resolved by an interactively-run boabot, nothing in `config.yaml` or `~/.boabot/credentials`. (`tasks.md` I2)
- [ ] Windows interactive: the same, via Credential Manager. (`tasks.md` I2)
- [ ] Linux interactive: the same, via Secret Service in a desktop session. (`tasks.md` I2)
- [ ] macOS LaunchDaemon: System keychain reachability for the keystore provider (FR-041's core open question). (`tasks.md` I2)
- [ ] Windows service: Credential Manager read/write under the service's own account identity (OQ-7). (`tasks.md` I2)
- [ ] Linux systemd unit: `LoadCredentialEncrypted=` → `$CREDENTIALS_DIRECTORY` resolution with no session D-Bus available. (`tasks.md` I2)
- [ ] Linux systemd unit, negative path: only a Secret Service entry present, no systemd credential — resolution fails with an error naming every provider consulted, not a hang. (`tasks.md` I2)
- [ ] `boabotctl secret set/get/delete` exercised for real on each of macOS, Windows, and Linux. (`tasks.md` C5, I2)
- [ ] Full three-OS CI matrix (`go build`/`go vet`/`golangci-lint run`/`go test -race ./...`, coverage ≥90%) actually green on macOS, Windows, and Linux runners — this job runs the suite on its own development platform only; the multi-OS CI matrix itself (`.github/workflows/` today is Linux-only) is infrastructure setup, not something a single dev-flow run can execute. (`tasks.md` I4)
- [ ] `internal/infrastructure/buzz/conn_integration_test.go`'s `TestDialLibRelay_LiveRelay` run for real against a live relay (`BUZZ_TEST_RELAY_URL` set) — currently only confirmed to compile under `-tags integration`. (`tasks.md` D3, I1)
- [ ] Live `buzz-relay`: confirm D7's `kind:0` profile publish (inside `Connect`, before NIP-42 authentication) actually succeeds against a real relay rather than being rejected `restricted:` for happening pre-auth -- the same class of issue F14's connect-vs-authenticate signal-timing fix (this phase) addresses for presence, but D7's publish itself was out of Phase F's scope to restructure. If it fails live, FR-011's AC needs Phase D revisited to move the profile publish after `Authenticate` as well. (`tasks.md` D7, F14, I1)
- [ ] Live `buzz-relay`: confirm kind:44100/44101 carry the channel UUID under an `h` tag as `handleMembershipEvent` assumes (`discovery.go`) -- unverified against the PRD, which documents these kinds' existence and `#p`-gating but not this specific tag name. A wrong assumption means F3's auto-subscribe/unsubscribe silently never fires. (`tasks.md` F3, I1)
- [ ] Live `buzz-relay`: confirm kind:39002 (member list) actually carries a `["p", <member-pubkey>]` tag per member, as `handleDiscoveryEvent`'s membership-gating (F1) assumes -- if the real member-list shape differs, F1 never triggers a kind:9 subscription for any channel at all. (`tasks.md` F1, I1)
- [ ] Live `buzz-relay`: end-to-end @mention → dispatch → threaded `kind:9` reply, confirmed rendering correctly (NIP-10 thread) in the Buzz desktop client. (`tasks.md` F5/F6/F11/F12, I1)
- [ ] Live `buzz-relay`: `kind:20001` presence observed staying under the 180s staleness bound over a real connection, including a manual relay-restart to confirm the suspend/resume-on-reconnect behavior (F14) end-to-end, not just against `SetConnStateFunc`'s unit-tested call sites. (`tasks.md` F14, I1)
- [ ] Live `buzz-relay`: `!shutdown` sent from an owner/allowlisted pubkey observed to actually trigger a clean process shutdown now that Phase H wires `WithShutdownFunc` to `team.TeamManager.Shutdown` (not `application.RunAgentUseCase.Shutdown` directly -- `main.go` has no reference to any single bot's `RunAgentUseCase`; `TeamManager.Shutdown` is the top-level hook it does have, and it already broadcasts `MessageTypeShutdown` to every registered bot, which is itself what triggers each bot's own `RunAgentUseCase.Shutdown`/monitor `Stop` sequence). This phase only proves the hook is wired and callable, not the full live-relay behavior. (`tasks.md` F17, H2)
- [ ] Live `buzz-relay`: reaction subscriptions (F18) -- since no F1–F18 task has Monitor actually subscribe to reactions, confirm (once a later phase adds reaction consumption) that the `RelayClient.Subscribe` guard's protection holds against a live relay, not just the fake-conn unit tests. (`tasks.md` F18)
- [ ] **Pin and record a validated `block/buzz` relay commit.** Not done this run -- see `docs/architectural-decision-record.md`'s ADR-B020. The first time this code is run against a live `buzz-relay` (Buzz's own `docker-compose.yml`), record the relay commit/version validated against, in the ADR, alongside the integration-test results below. (`tasks.md` H4, I1)
- [ ] **Multiple bots in one `team.yaml` sharing the Buzz-enabled process.** `application.RunAgentUseCase.Run` calls `Start` on every registered `domain.ChannelMonitor` (the shared `tm.monitors` slice) once per bot goroutine it manages -- an existing pattern from before this feature, also true of `slack.Monitor`, not something Phase H changed. With N bots in `team.yaml`, the Buzz `Monitor.Start`/`AcquireLock` sequence therefore runs N times in the same process: the first call acquires the FR-031 lock and proceeds normally; the 2nd..Nth calls hit the same-process lock (a different PID never comes into play) and log "another boabot process is likely already running against this nsec" -- functionally harmless (first wins, Buzz still starts exactly once) but a misleading log line in a genuinely intra-process, not inter-process, situation. Confirm this reads acceptably in a real multi-bot `team.yaml` deployment; fixing the log wording (distinguishing same-process-double-Start from cross-process contention) would require changing `internal/infrastructure/buzz/monitor.go`, out of this phase's scope per the launching brief's explicit "flag it, don't edit buzz/" instruction. (`tasks.md` H2)
- [ ] **File the upstream `checkptr`/`unsafe.Pointer` bug against `fiatjaf.com/nostr`.** `event.go`'s `appendJSONString`/`writeJSONString` (both call sites, ~line 195/241) store `base := uintptr(unsafe.Pointer(unsafe.StringData(s)))` across statements and later reconstruct `unsafe.Pointer(base + uintptr(i))` — a rule-1 `unsafe.Pointer` violation that `go test -race`'s auto-enabled `checkptr` instrumentation catches, non-deterministically (GC-timing dependent), as a hard `fatal error: checkptr: pointer arithmetic result points to invalid allocation` process abort whenever an event is signed/verified while any other goroutine is alive. Confirmed empirically in this phase — see `research.md`'s "Critical finding" writeup for the full repro history. Every CI `go test -race` invocation in this repo (from this phase onward, since `internal/infrastructure/buzz` is now part of `./...`) **must** pass `-gcflags=all=-d=checkptr=0` until this is fixed upstream or a patched version is pinned; `.github/workflows/boabot.yml` needs this flag added (out of this phase's scope — Phase C/H's `main.go`/CI wiring, or a follow-up — but flagged here so it isn't missed).

## Edge Cases & Solutions

- **Reconnect cannot reuse a `*nostr.Relay`.** See `research.md`'s D1 findings — `reconnect.go` always dials fresh via the `dialFunc` seam rather than attempting to reset/reconnect an existing one.
- **A caller's subscription channel must survive reconnect, but the library's own channel can't.** Solved by `RelayClient` owning the caller-facing channel and re-attaching a fresh library subscription to it on every reconnect (`attachSub`/`resubscribeAll`, D8).
- **Closing a subscription channel race-free.** A pump goroutine could in principle still be sending to `out` at the moment either `Close()` or a ctx-cancel-triggered `removeAndClose` decides to close it. Solved with a per-attach-generation `pumpDone` channel, only created while holding `subMu`, and only closed by the pump itself right before it returns; both `Close()` (via the global `pumpWG.Wait()`) and `removeAndClose` (via that specific `pumpDone`) wait for in-flight sends to finish before closing `out`. No `sync.WaitGroup` `Add`-after-`Wait` hazard, because entry registration/removal and pump-attach are all serialized through the same `subMu`.
- **NIP-42 AUTH's real result is only observable via an explicit call, not `RelayOptions.AuthHandler`.** See `research.md`'s D1 findings for why; `authenticateOn` calls `(*Relay).Auth` explicitly and retries only on the specific "challenge not yet arrived" condition.

## Deviations from Plan

- **`RelayOptions.AuthHandler` not used**, despite being the library's "intended" auto-auth mechanism — its return value is discarded internally, making it unusable for FR-009's later invalid/restricted classification. `Authenticate` calls `(*Relay).Auth` explicitly instead. Recorded as a deliberate deviation from the "obvious" library-idiomatic approach, not an oversight — see `research.md`.
- **`go test -race` requires `-gcflags=all=-d=checkptr=0`** for any package (now including `./...` repo-wide) that touches `internal/infrastructure/buzz`, due to the upstream `fiatjaf.com/nostr` bug documented above and in `research.md`. This was not anticipated in `plan.md`/`tasks.md` and is a necessary addition to this run's verification commands, not a plan change to the code itself.

## Lessons Learned

- **Read the target library's actual source for lifecycle constraints before designing a test seam, not just its call signatures.** The D1 spike's real payoff wasn't "what are the method names" (that part was quick) — it was discovering the two non-obvious constraints (`*nostr.Relay` is single-use; `Subscription.Events` dies at disconnect) that would have silently broken FR-012's reconnect requirement if the seam had been designed around a naive "wrap the whole `*nostr.Relay`" abstraction instead of the narrower `relayConn` behavior-boundary seam actually used.
- **`-race`'s `checkptr` diagnostic can catch bugs in dependencies, not just your own code — and the fix isn't always yours to make.** Confirming this was real (not flaky test infrastructure) required isolating the repro down to "any concurrent goroutine + any event signing," which took several throwaway loop tests before the pattern was clear. Worth the time: silently ignoring an intermittent `fatal error` would have left a landmine for whichever later phase's test happened to trip it.
